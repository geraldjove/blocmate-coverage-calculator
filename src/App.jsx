import Home from './pages/Home'

// Standalone single-page app — the calculator renders directly, with no
// base44 SDK, auth gate, router, or backend dependencies.
export default function App() {
  return <Home />
}
